DATABASE_URL="postgresql://cyasset:cyasset@localhost:5434/cyasset"
AUTH_SECRET="generate-with-npx-auth-secret"
# Basis URL publik yang di-encode ke QR Code (lihat src/lib/qr.ts) — isi dengan
# domain resmi sekolah saat deploy production, http://localhost:3000 untuk dev.
APP_URL="http://localhost:3000"
